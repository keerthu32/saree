# VSM System

A simple PHP Vehicle Service Management starter structure.

## Setup
1. Create DB with `database/vsm.sql`
2. Update credentials in `config/config.php`
3. Serve with PHP built-in server:
   ```bash
   php -S localhost:8000 -t vsm-system
   ```
4. Login with `admin@example.com / password`
